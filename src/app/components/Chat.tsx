import { Accordion } from "@/components/ui/accordion";

const Chat = () => {
  return (
    <Accordion
      type="single"
      collapsible
      className="relative bg-white z-40 shadow"
    >
      
    </Accordion>
  );
};

export default Chat;
